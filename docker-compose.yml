services:
  web:
    build: .
    command: python -m app.web
    ports:
      - "8000:8000"
    env_file:
      - .env
  worker:
    build: .
    command: python -m app.worker
    env_file:
      - .env
